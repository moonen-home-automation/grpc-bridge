---
name: Release Drafter

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main

jobs:
  workflows:
    uses: moonen-home-automation/workflows/.github/workflows/release-drafter.yaml@main